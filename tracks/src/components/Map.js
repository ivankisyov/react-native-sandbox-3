import React from "react";
import { Text, Input, Button } from "react-native-elements";
import { View, StyleSheet } from "react-native";

const Map = () => {
  return <Text h3>Map</Text>;
};

const styles = StyleSheet.create({});

export default Map;
